import React from 'react';

//Function Component
/*
const Button = function (props) {
    return (
        <button>{props.label}</button>
    );
};
*/

const Button = (props) =>{
    return (
        <button>{props.label}</button>
    );
};

export default Button;